/**
 * Senzing Data Mart Reports REST API
 * This is the Senzing Data Mart Reports API.  This provides endpoints and types for obtaining reports from the data mart.
 *
 * OpenAPI spec version: 2.0.0-beta.1.0
 * 
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 * Do not edit the class manually.
 */
import { SzMatchCounts } from './szMatchCounts';
import { SzRelationCounts } from './szRelationCounts';

/**
 * Describes the cross-source statistics between two data sources.
 */
export interface SzCrossSourceSummary { 
    /**
     * The primary data source in the cross comparison.
     */
    dataSource: string;
    /**
     * The versus data source in the cross comparison.
     */
    versusDataSource: string;
    /**
     * The array of `SzMatchCounts` describing the entity and record counts  associated with matches from records of the primary data source to at  least one record of the \"versus\" data source for each combination of  match key and principle that were requested.  If all were requested, this includes the cases where either or both of the match key and principle are absent or `null` indicating tracking across all match keys and/or principles.
     */
    matches: Array<SzMatchCounts>;
    /**
     * The array of `SzRelationCounts` describing the entity, record and relationship counts associated with ambiguous matches from entities having at least one record from the primary data source to entities having at least at least one record from the \"versus\" data source for each combination of match key and principle that were requested. If all were requested, this includes the cases where either or both of the match key and principle are absent or `null` indicating tracking across all match keys and/or principles.
     */
    ambiguousMatches: Array<SzRelationCounts>;
    /**
     * The array of `SzRelationCounts` describing the entity, record and relationship counts associated with possible matches from entities having at least one record from the primary data source to entities having at least one record from the \"versus\" data source for each combination of match key and principle that were requested.  If all were requested, this includes the cases where either or both of the match key and principle are absent or `null` indicating tracking across all match keys and/or principles.
     */
    possibleMatches: Array<SzRelationCounts>;
    /**
     * The array of `SzRelationCounts` describing the entity, record and relationship counts associated with possible relations from entities having at least one record from the primary data source to entities having at least one record from the \"versus\" data source for each combination of match key and principle that were requested.  If all were requested, this includes the cases where either or both of the match key and principle are absent or `null` indicating tracking across all match keys and/or principles.
     */
    possibleRelations: Array<SzRelationCounts>;
    /**
     * The array of `SzRelationCounts` describing the entity, record and relationship counts associated with disclosed relations from entities having at least one record from the primary data source to entities having at least one record from the \"versus\" data source for each combination of match key and principle that were requested.  If all were requested, this includes the cases where either or both of the match key and principle are absent or `null` indicating tracking across all match keys and/or principles.
     */
    disclosedRelations: Array<SzRelationCounts>;
}